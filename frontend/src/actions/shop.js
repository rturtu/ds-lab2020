import Reflux from 'reflux';

// ShopActions will be empty since we will only
// load the items in the store once and they will
// be there forever
export const ShopActions = Reflux.createActions([
])

